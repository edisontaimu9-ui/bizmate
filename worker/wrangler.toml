name = "bizmate"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Non-secret config (safe to commit)
[vars]
FIREBASE_PROJECT_ID = "bizmate-3a18b"
SUPABASE_URL = "https://jjtvgaubalixqfnwgdoq.supabase.co"
# Optional — only needed if you're not using Groq's default model/endpoint.
# LLM_BASE_URL = "https://api.groq.com/openai/v1"
# LLM_MODEL = "openai/gpt-oss-120b"

# Secrets — set these with:
#   npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY
#   npx wrangler secret put LLM_API_KEY          (a Groq API key by default)
#   npx wrangler secret put WHATSAPP_ACCESS_TOKEN
#   npx wrangler secret put WHATSAPP_PHONE_NUMBER_ID
#   npx wrangler secret put WHATSAPP_WEBHOOK_VERIFY_TOKEN   (a value YOU choose — must match what you enter in Meta's dashboard)
#   npx wrangler secret put WHATSAPP_APP_SECRET             (from Meta app dashboard > App settings > Basic — verifies inbound webhook requests are really from Meta)
# Do NOT put secret values in this file.
